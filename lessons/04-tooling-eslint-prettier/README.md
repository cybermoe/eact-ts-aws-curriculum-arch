# Lesson 04 – Tooling: ESLint + Prettier + Structure

**Objectives**
- Add ESLint + Prettier to a React+TS project.
- Enforce code quality and consistent formatting.

**Commands**
```bash
npm install
npm run lint
npm run format
```
